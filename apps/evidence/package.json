{
  "name": "hpt-evidence",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run sources && node scripts/clean-dev-state.mjs && evidence dev --open /",
    "sources": "evidence sources && node scripts/fix-empty-parquet-sources.mjs",
    "build": "evidence build",
    "preview": "evidence preview"
  },
  "engines": {
    "node": "20 || 22",
    "npm": ">=7.0.0"
  },
  "dependencies": {
    "@evidence-dev/core-components": "^5.4.2",
    "@evidence-dev/duckdb": "^2.0.1",
    "@evidence-dev/evidence": "^40.1.8"
  },
  "devDependencies": {
    "typescript": ">=5.0.0 <6.0.0"
  }
}
