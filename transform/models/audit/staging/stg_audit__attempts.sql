select
    run_id,
    cast(run_date as date) as run_date,
    attempt_id,
    attempt_ordinal,
    attempt_type,
    hospital_id,
    snapshot_id,
    snapshot_ids,
    source_url,
    source_file_name,
    file_hash,
    started_at,
    ended_at,
    elapsed_s,
    status,
    failure_category,
    failure_message,
    stage_statuses,
    stage_elapsed_s,
    download_outcome,
    http_status,
    content_length,
    last_modified,
    etag,
    bytes_downloaded,
    hash_changed,
    raw_path,
    compression,
    content_format,
    detected_layout,
    schema_version,
    parser_class,
    parser_path,
    bronze_row_counts,
    quarantine_counts,
    dbt_action,
    dbt_command,
    dbt_selector,
    dbt_full_refresh,
    peak_rss_mb
from {{ source('audit', 'attempts') }}
