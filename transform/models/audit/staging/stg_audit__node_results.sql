select
    run_id,
    cast(run_date as date) as run_date,
    attempt_id,
    node_unique_id,
    node_name,
    resource_type,
    package_name,
    materialization,
    node_schema,
    tags,
    node_status,
    message,
    test_failures,
    execution_time_s,
    compile_elapsed_s,
    execute_elapsed_s,
    started_at,
    ended_at,
    rows_affected,
    adapter_code,
    thread_id,
    dbt_command,
    dbt_selector,
    dbt_full_refresh,
    snapshot_ids,
    snapshot_count
from {{ source('audit', 'node_results') }}
